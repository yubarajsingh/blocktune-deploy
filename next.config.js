/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : [
      "images.unsplash.com",
      "tailwindcss.com",
      "tailwindui.com"
    ]
  }
}

module.exports = nextConfig
